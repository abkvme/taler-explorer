# Build output
/.bin/
/dist/
# Anchored so we only ignore the binary at the repo root, NOT the source
# package at cmd/taler-explorer/.
/taler-explorer
*.exe

# Local SQLite DB + sidecars
*.db
*.db-wal
*.db-shm

# Local dev config (contains RPC credentials). config.example.toml stays tracked.
config.toml

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Go test + coverage
*.out
coverage.*

# Logs
*.log
